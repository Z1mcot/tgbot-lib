// Auto-generated Telegram Bot API models for C++
// Generated by telegram-api-generator. Do not edit manually.

#include <tgbot/net/Api.hpp>
#include <nlohmann/json.hpp>

namespace TgBot {
    using json = nlohmann::json;

    Api::Api(std::string api_key, HttpClient& http_client, std::string url)
        : api_key_(std::move(api_key)), http_client_(http_client), url_(std::move(url)) {
    }
    template<typename T>

    T Api::parseResponse(const std::string& response_str) const {
        if (response_str.compare(0, 6, "<html>") == 0) {
            throw TgException("Received HTML response from Telegram API, likely an error page. Response: " + response_str, TgException::ErrorCode::HtmlResponse);
        }

        try {
            json::parse(response_str);
        } catch (const json::parse_error& e) {
            throw TgException("Failed to parse JSON response: " + std::string(e.what()) + ". Response: " + response_str, TgException::ErrorCode::InvalidJson);
        }

        bool isOk = j["ok"].get<bool>();

        if (!isOk) {
            auto descriptionIt = j.find("description");
            auto errorCodeIt = j.find("error_code");

            std::string description = descriptionIt != j.end() ? descriptionIt->get<std::string>() : "Unknown error";
            int errorCode = errorCodeIt != j.end() ? errorCodeIt->get<int>() : 0;
            TgException::ErrorCode tgErrorCode = static_cast<TgException::ErrorCode>(errorCode);
            throw TgException(description, tgErrorCode);
        }

        auto resultIt = j.find("result");

        if (resultIt == j.end()) {
            throw TgException("Response JSON does not contain 'result' field. Response: " + response_str, TgException::ErrorCode::InvalidJson);
        }
        return resultIt->get<T>();
    }


    // Getting updates

    coro::task<std::vector<Update::Ptr>> Api::getUpdates(std::int64_t offset, std::int64_t limit, std::int64_t timeout, const std::vector<std::string>& allowed_updates) const {
        TgBot::GetUpdatesRequest request{};
        request.offset = offset;
        request.limit = limit;
        request.timeout = timeout;
        request.allowed_updates = allowed_updates;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUpdates";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<Update::Ptr>>(response);
    }

    coro::task<std::vector<Update::Ptr>> Api::getUpdates(const GetUpdatesRequest& request) const {
         request{};
        request.offset = offset;
        request.limit = limit;
        request.timeout = timeout;
        request.allowed_updates = allowed_updates;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUpdates";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<Update::Ptr>>(response);
    }

    coro::task<bool> Api::setWebhook(const std::string& url, InputFile::Ptr certificate, const std::string& ip_address, std::int64_t max_connections, const std::vector<std::string>& allowed_updates, bool drop_pending_updates, const std::string& secret_token) const {
        TgBot::SetWebhookRequest request{};
        request.url = url;
        request.certificate = certificate;
        request.ip_address = ip_address;
        request.max_connections = max_connections;
        request.allowed_updates = allowed_updates;
        request.drop_pending_updates = drop_pending_updates;
        request.secret_token = secret_token;
        // Generate multipart form data for file uploads
        std::string boundary = "----TelegramBotAPI123456789";
        std::string body = toMultipart(request, boundary);
        std::string contentType = "multipart/form-data; boundary=" + boundary;
        const std::string target = "/bot" + api_key_ + "/setWebhook";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setWebhook(const SetWebhookRequest& request) const {
         request{};
        request.url = url;
        request.certificate = certificate;
        request.ip_address = ip_address;
        request.max_connections = max_connections;
        request.allowed_updates = allowed_updates;
        request.drop_pending_updates = drop_pending_updates;
        request.secret_token = secret_token;
        // Generate multipart form data for file uploads
        std::string boundary = "----TelegramBotAPI123456789";
        std::string body = toMultipart(request, boundary);
        std::string contentType = "multipart/form-data; boundary=" + boundary;
        const std::string target = "/bot" + api_key_ + "/setWebhook";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteWebhook(bool drop_pending_updates) const {
        TgBot::DeleteWebhookRequest request{};
        request.drop_pending_updates = drop_pending_updates;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteWebhook";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteWebhook(const DeleteWebhookRequest& request) const {
         request{};
        request.drop_pending_updates = drop_pending_updates;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteWebhook";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<WebhookInfo::Ptr> Api::getWebhookInfo() const {
        const std::string target = "/bot" + api_key_ + "/getWebhookInfo";
        std::string response = co_await http_client_.makeRequest(HttpVerb::GET, target);
        co_return parseResponse<WebhookInfo::Ptr>(response);
    }


    // Available methods

    coro::task<User::Ptr> Api::getMe() const {
        const std::string target = "/bot" + api_key_ + "/getMe";
        std::string response = co_await http_client_.makeRequest(HttpVerb::GET, target);
        co_return parseResponse<User::Ptr>(response);
    }

    coro::task<bool> Api::logOut() const {
        const std::string target = "/bot" + api_key_ + "/logOut";
        std::string response = co_await http_client_.makeRequest(HttpVerb::GET, target);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::close() const {
        const std::string target = "/bot" + api_key_ + "/close";
        std::string response = co_await http_client_.makeRequest(HttpVerb::GET, target);
        co_return parseResponse<bool>(response);
    }

    coro::task<Message::Ptr> Api::sendMessage(std::int64_t chat_id, const std::string& text, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& entities, LinkPreviewOptions::Ptr link_preview_options, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendMessageRequest request{};
        request.chat_id = chat_id;
        request.text = text;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.parse_mode = parse_mode;
        request.entities = entities;
        request.link_preview_options = link_preview_options;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendMessage(const SendMessageRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.text = text;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.parse_mode = parse_mode;
        request.entities = entities;
        request.link_preview_options = link_preview_options;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::forwardMessage(std::int64_t chat_id, std::int64_t from_chat_id, std::int64_t message_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, std::int64_t video_start_timestamp, bool disable_notification, bool protect_content, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters) const {
        TgBot::ForwardMessageRequest request{};
        request.chat_id = chat_id;
        request.from_chat_id = from_chat_id;
        request.message_id = message_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.video_start_timestamp = video_start_timestamp;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/forwardMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::forwardMessage(const ForwardMessageRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.from_chat_id = from_chat_id;
        request.message_id = message_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.video_start_timestamp = video_start_timestamp;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/forwardMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<std::vector<MessageId::Ptr>> Api::forwardMessages(std::int64_t chat_id, std::int64_t from_chat_id, const std::vector<std::int64_t>& message_ids, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, bool disable_notification, bool protect_content) const {
        TgBot::ForwardMessagesRequest request{};
        request.chat_id = chat_id;
        request.from_chat_id = from_chat_id;
        request.message_ids = message_ids;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/forwardMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<MessageId::Ptr>>(response);
    }

    coro::task<std::vector<MessageId::Ptr>> Api::forwardMessages(const ForwardMessagesRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.from_chat_id = from_chat_id;
        request.message_ids = message_ids;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/forwardMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<MessageId::Ptr>>(response);
    }

    coro::task<MessageId::Ptr> Api::copyMessage(std::int64_t chat_id, std::int64_t from_chat_id, std::int64_t message_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, std::int64_t video_start_timestamp, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, bool show_caption_above_media, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::CopyMessageRequest request{};
        request.chat_id = chat_id;
        request.from_chat_id = from_chat_id;
        request.message_id = message_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.video_start_timestamp = video_start_timestamp;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/copyMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<MessageId::Ptr>(response);
    }

    coro::task<MessageId::Ptr> Api::copyMessage(const CopyMessageRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.from_chat_id = from_chat_id;
        request.message_id = message_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.video_start_timestamp = video_start_timestamp;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/copyMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<MessageId::Ptr>(response);
    }

    coro::task<std::vector<MessageId::Ptr>> Api::copyMessages(std::int64_t chat_id, std::int64_t from_chat_id, const std::vector<std::int64_t>& message_ids, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, bool disable_notification, bool protect_content, bool remove_caption) const {
        TgBot::CopyMessagesRequest request{};
        request.chat_id = chat_id;
        request.from_chat_id = from_chat_id;
        request.message_ids = message_ids;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.remove_caption = remove_caption;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/copyMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<MessageId::Ptr>>(response);
    }

    coro::task<std::vector<MessageId::Ptr>> Api::copyMessages(const CopyMessagesRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.from_chat_id = from_chat_id;
        request.message_ids = message_ids;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.remove_caption = remove_caption;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/copyMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<MessageId::Ptr>>(response);
    }

    coro::task<Message::Ptr> Api::sendPhoto(std::int64_t chat_id, const std::string& photo, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, bool show_caption_above_media, bool has_spoiler, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendPhotoRequest request{};
        request.chat_id = chat_id;
        request.photo = photo;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.has_spoiler = has_spoiler;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendPhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendPhoto(const SendPhotoRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.photo = photo;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.has_spoiler = has_spoiler;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendPhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendAudio(std::int64_t chat_id, const std::string& audio, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, std::int64_t duration, const std::string& performer, const std::string& title, const std::string& thumbnail, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendAudioRequest request{};
        request.chat_id = chat_id;
        request.audio = audio;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.duration = duration;
        request.performer = performer;
        request.title = title;
        request.thumbnail = thumbnail;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendAudio";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendAudio(const SendAudioRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.audio = audio;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.duration = duration;
        request.performer = performer;
        request.title = title;
        request.thumbnail = thumbnail;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendAudio";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendDocument(std::int64_t chat_id, const std::string& document, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& thumbnail, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, bool disable_content_type_detection, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendDocumentRequest request{};
        request.chat_id = chat_id;
        request.document = document;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.thumbnail = thumbnail;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.disable_content_type_detection = disable_content_type_detection;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendDocument";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendDocument(const SendDocumentRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.document = document;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.thumbnail = thumbnail;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.disable_content_type_detection = disable_content_type_detection;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendDocument";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendVideo(std::int64_t chat_id, const std::string& video, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, std::int64_t duration, std::int64_t width, std::int64_t height, const std::string& thumbnail, const std::string& cover, std::int64_t start_timestamp, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, bool show_caption_above_media, bool has_spoiler, bool supports_streaming, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendVideoRequest request{};
        request.chat_id = chat_id;
        request.video = video;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.duration = duration;
        request.width = width;
        request.height = height;
        request.thumbnail = thumbnail;
        request.cover = cover;
        request.start_timestamp = start_timestamp;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.has_spoiler = has_spoiler;
        request.supports_streaming = supports_streaming;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendVideo";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendVideo(const SendVideoRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.video = video;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.duration = duration;
        request.width = width;
        request.height = height;
        request.thumbnail = thumbnail;
        request.cover = cover;
        request.start_timestamp = start_timestamp;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.has_spoiler = has_spoiler;
        request.supports_streaming = supports_streaming;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendVideo";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendAnimation(std::int64_t chat_id, const std::string& animation, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, std::int64_t duration, std::int64_t width, std::int64_t height, const std::string& thumbnail, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, bool show_caption_above_media, bool has_spoiler, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendAnimationRequest request{};
        request.chat_id = chat_id;
        request.animation = animation;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.duration = duration;
        request.width = width;
        request.height = height;
        request.thumbnail = thumbnail;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.has_spoiler = has_spoiler;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendAnimation";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendAnimation(const SendAnimationRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.animation = animation;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.duration = duration;
        request.width = width;
        request.height = height;
        request.thumbnail = thumbnail;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.has_spoiler = has_spoiler;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendAnimation";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendVoice(std::int64_t chat_id, const std::string& voice, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, std::int64_t duration, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendVoiceRequest request{};
        request.chat_id = chat_id;
        request.voice = voice;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.duration = duration;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendVoice";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendVoice(const SendVoiceRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.voice = voice;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.duration = duration;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendVoice";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendVideoNote(std::int64_t chat_id, const std::string& video_note, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, std::int64_t duration, std::int64_t length, const std::string& thumbnail, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendVideoNoteRequest request{};
        request.chat_id = chat_id;
        request.video_note = video_note;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.duration = duration;
        request.length = length;
        request.thumbnail = thumbnail;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendVideoNote";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendVideoNote(const SendVideoNoteRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.video_note = video_note;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.duration = duration;
        request.length = length;
        request.thumbnail = thumbnail;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendVideoNote";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendPaidMedia(std::int64_t chat_id, std::int64_t star_count, const std::vector<InputPaidMedia::Ptr>& media, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& payload, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, bool show_caption_above_media, bool disable_notification, bool protect_content, bool allow_paid_broadcast, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendPaidMediaRequest request{};
        request.chat_id = chat_id;
        request.star_count = star_count;
        request.media = media;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.payload = payload;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendPaidMedia";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendPaidMedia(const SendPaidMediaRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.star_count = star_count;
        request.media = media;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.payload = payload;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendPaidMedia";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<std::vector<Message::Ptr>> Api::sendMediaGroup(std::int64_t chat_id, const std::vector<InputMedia::Ptr>& media, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, ReplyParameters::Ptr reply_parameters) const {
        TgBot::SendMediaGroupRequest request{};
        request.chat_id = chat_id;
        request.media = media;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.reply_parameters = reply_parameters;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendMediaGroup";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<Message::Ptr>>(response);
    }

    coro::task<std::vector<Message::Ptr>> Api::sendMediaGroup(const SendMediaGroupRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.media = media;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.reply_parameters = reply_parameters;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendMediaGroup";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<Message::Ptr>>(response);
    }

    coro::task<Message::Ptr> Api::sendLocation(std::int64_t chat_id, double latitude, double longitude, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, double horizontal_accuracy, std::int64_t live_period, std::int64_t heading, std::int64_t proximity_alert_radius, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendLocationRequest request{};
        request.chat_id = chat_id;
        request.latitude = latitude;
        request.longitude = longitude;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.horizontal_accuracy = horizontal_accuracy;
        request.live_period = live_period;
        request.heading = heading;
        request.proximity_alert_radius = proximity_alert_radius;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendLocation";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendLocation(const SendLocationRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.latitude = latitude;
        request.longitude = longitude;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.horizontal_accuracy = horizontal_accuracy;
        request.live_period = live_period;
        request.heading = heading;
        request.proximity_alert_radius = proximity_alert_radius;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendLocation";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendVenue(std::int64_t chat_id, double latitude, double longitude, const std::string& title, const std::string& address, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& foursquare_id, const std::string& foursquare_type, const std::string& google_place_id, const std::string& google_place_type, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendVenueRequest request{};
        request.chat_id = chat_id;
        request.latitude = latitude;
        request.longitude = longitude;
        request.title = title;
        request.address = address;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.foursquare_id = foursquare_id;
        request.foursquare_type = foursquare_type;
        request.google_place_id = google_place_id;
        request.google_place_type = google_place_type;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendVenue";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendVenue(const SendVenueRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.latitude = latitude;
        request.longitude = longitude;
        request.title = title;
        request.address = address;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.foursquare_id = foursquare_id;
        request.foursquare_type = foursquare_type;
        request.google_place_id = google_place_id;
        request.google_place_type = google_place_type;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendVenue";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendContact(std::int64_t chat_id, const std::string& phone_number, const std::string& first_name, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& last_name, const std::string& vcard, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendContactRequest request{};
        request.chat_id = chat_id;
        request.phone_number = phone_number;
        request.first_name = first_name;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.last_name = last_name;
        request.vcard = vcard;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendContact";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendContact(const SendContactRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.phone_number = phone_number;
        request.first_name = first_name;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.last_name = last_name;
        request.vcard = vcard;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendContact";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendPoll(std::int64_t chat_id, const std::string& question, const std::vector<InputPollOption::Ptr>& options, const std::string& business_connection_id, std::int64_t message_thread_id, const std::string& question_parse_mode, const std::vector<MessageEntity::Ptr>& question_entities, bool is_anonymous, const std::string& type_, bool allows_multiple_answers, std::int64_t correct_option_id, const std::string& explanation, const std::string& explanation_parse_mode, const std::vector<MessageEntity::Ptr>& explanation_entities, std::int64_t open_period, std::int64_t close_date, bool is_closed, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendPollRequest request{};
        request.chat_id = chat_id;
        request.question = question;
        request.options = options;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.question_parse_mode = question_parse_mode;
        request.question_entities = question_entities;
        request.is_anonymous = is_anonymous;
        request.type_ = type_;
        request.allows_multiple_answers = allows_multiple_answers;
        request.correct_option_id = correct_option_id;
        request.explanation = explanation;
        request.explanation_parse_mode = explanation_parse_mode;
        request.explanation_entities = explanation_entities;
        request.open_period = open_period;
        request.close_date = close_date;
        request.is_closed = is_closed;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendPoll";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendPoll(const SendPollRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.question = question;
        request.options = options;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.question_parse_mode = question_parse_mode;
        request.question_entities = question_entities;
        request.is_anonymous = is_anonymous;
        request.type_ = type_;
        request.allows_multiple_answers = allows_multiple_answers;
        request.correct_option_id = correct_option_id;
        request.explanation = explanation;
        request.explanation_parse_mode = explanation_parse_mode;
        request.explanation_entities = explanation_entities;
        request.open_period = open_period;
        request.close_date = close_date;
        request.is_closed = is_closed;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendPoll";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendChecklist(const std::string& business_connection_id, std::int64_t chat_id, InputChecklist::Ptr checklist, bool disable_notification, bool protect_content, const std::string& message_effect_id, ReplyParameters::Ptr reply_parameters, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::SendChecklistRequest request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.checklist = checklist;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.message_effect_id = message_effect_id;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendChecklist";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendChecklist(const SendChecklistRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.checklist = checklist;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.message_effect_id = message_effect_id;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendChecklist";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendDice(std::int64_t chat_id, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& emoji, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendDiceRequest request{};
        request.chat_id = chat_id;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.emoji = emoji;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendDice";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendDice(const SendDiceRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.emoji = emoji;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendDice";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<bool> Api::sendMessageDraft(std::int64_t chat_id, std::int64_t draft_id, const std::string& text, std::int64_t message_thread_id, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& entities) const {
        TgBot::SendMessageDraftRequest request{};
        request.chat_id = chat_id;
        request.draft_id = draft_id;
        request.text = text;
        request.message_thread_id = message_thread_id;
        request.parse_mode = parse_mode;
        request.entities = entities;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendMessageDraft";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::sendMessageDraft(const SendMessageDraftRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.draft_id = draft_id;
        request.text = text;
        request.message_thread_id = message_thread_id;
        request.parse_mode = parse_mode;
        request.entities = entities;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendMessageDraft";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::sendChatAction(std::int64_t chat_id, const std::string& action, const std::string& business_connection_id, std::int64_t message_thread_id) const {
        TgBot::SendChatActionRequest request{};
        request.chat_id = chat_id;
        request.action = action;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendChatAction";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::sendChatAction(const SendChatActionRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.action = action;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendChatAction";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setMessageReaction(std::int64_t chat_id, std::int64_t message_id, const std::vector<ReactionType::Ptr>& reaction, bool is_big) const {
        TgBot::SetMessageReactionRequest request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.reaction = reaction;
        request.is_big = is_big;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMessageReaction";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setMessageReaction(const SetMessageReactionRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.reaction = reaction;
        request.is_big = is_big;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMessageReaction";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<UserProfilePhotos::Ptr> Api::getUserProfilePhotos(std::int64_t user_id, std::int64_t offset, std::int64_t limit) const {
        TgBot::GetUserProfilePhotosRequest request{};
        request.user_id = user_id;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUserProfilePhotos";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<UserProfilePhotos::Ptr>(response);
    }

    coro::task<UserProfilePhotos::Ptr> Api::getUserProfilePhotos(const GetUserProfilePhotosRequest& request) const {
         request{};
        request.user_id = user_id;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUserProfilePhotos";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<UserProfilePhotos::Ptr>(response);
    }

    coro::task<UserProfileAudios::Ptr> Api::getUserProfileAudios(std::int64_t user_id, std::int64_t offset, std::int64_t limit) const {
        TgBot::GetUserProfileAudiosRequest request{};
        request.user_id = user_id;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUserProfileAudios";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<UserProfileAudios::Ptr>(response);
    }

    coro::task<UserProfileAudios::Ptr> Api::getUserProfileAudios(const GetUserProfileAudiosRequest& request) const {
         request{};
        request.user_id = user_id;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUserProfileAudios";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<UserProfileAudios::Ptr>(response);
    }

    coro::task<bool> Api::setUserEmojiStatus(std::int64_t user_id, const std::string& emoji_status_custom_emoji_id, std::int64_t emoji_status_expiration_date) const {
        TgBot::SetUserEmojiStatusRequest request{};
        request.user_id = user_id;
        request.emoji_status_custom_emoji_id = emoji_status_custom_emoji_id;
        request.emoji_status_expiration_date = emoji_status_expiration_date;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setUserEmojiStatus";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setUserEmojiStatus(const SetUserEmojiStatusRequest& request) const {
         request{};
        request.user_id = user_id;
        request.emoji_status_custom_emoji_id = emoji_status_custom_emoji_id;
        request.emoji_status_expiration_date = emoji_status_expiration_date;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setUserEmojiStatus";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<File::Ptr> Api::getFile(const std::string& file_id) const {
        TgBot::GetFileRequest request{};
        request.file_id = file_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getFile";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<File::Ptr>(response);
    }

    coro::task<File::Ptr> Api::getFile(const GetFileRequest& request) const {
         request{};
        request.file_id = file_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getFile";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<File::Ptr>(response);
    }

    coro::task<bool> Api::banChatMember(std::int64_t chat_id, std::int64_t user_id, std::int64_t until_date, bool revoke_messages) const {
        TgBot::BanChatMemberRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.until_date = until_date;
        request.revoke_messages = revoke_messages;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/banChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::banChatMember(const BanChatMemberRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.until_date = until_date;
        request.revoke_messages = revoke_messages;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/banChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unbanChatMember(std::int64_t chat_id, std::int64_t user_id, bool only_if_banned) const {
        TgBot::UnbanChatMemberRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.only_if_banned = only_if_banned;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unbanChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unbanChatMember(const UnbanChatMemberRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.only_if_banned = only_if_banned;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unbanChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::restrictChatMember(std::int64_t chat_id, std::int64_t user_id, ChatPermissions::Ptr permissions, bool use_independent_chat_permissions, std::int64_t until_date) const {
        TgBot::RestrictChatMemberRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.permissions = permissions;
        request.use_independent_chat_permissions = use_independent_chat_permissions;
        request.until_date = until_date;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/restrictChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::restrictChatMember(const RestrictChatMemberRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.permissions = permissions;
        request.use_independent_chat_permissions = use_independent_chat_permissions;
        request.until_date = until_date;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/restrictChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::promoteChatMember(std::int64_t chat_id, std::int64_t user_id, bool is_anonymous, bool can_manage_chat, bool can_delete_messages, bool can_manage_video_chats, bool can_restrict_members, bool can_promote_members, bool can_change_info, bool can_invite_users, bool can_post_stories, bool can_edit_stories, bool can_delete_stories, bool can_post_messages, bool can_edit_messages, bool can_pin_messages, bool can_manage_topics, bool can_manage_direct_messages, bool can_manage_tags) const {
        TgBot::PromoteChatMemberRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.is_anonymous = is_anonymous;
        request.can_manage_chat = can_manage_chat;
        request.can_delete_messages = can_delete_messages;
        request.can_manage_video_chats = can_manage_video_chats;
        request.can_restrict_members = can_restrict_members;
        request.can_promote_members = can_promote_members;
        request.can_change_info = can_change_info;
        request.can_invite_users = can_invite_users;
        request.can_post_stories = can_post_stories;
        request.can_edit_stories = can_edit_stories;
        request.can_delete_stories = can_delete_stories;
        request.can_post_messages = can_post_messages;
        request.can_edit_messages = can_edit_messages;
        request.can_pin_messages = can_pin_messages;
        request.can_manage_topics = can_manage_topics;
        request.can_manage_direct_messages = can_manage_direct_messages;
        request.can_manage_tags = can_manage_tags;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/promoteChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::promoteChatMember(const PromoteChatMemberRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.is_anonymous = is_anonymous;
        request.can_manage_chat = can_manage_chat;
        request.can_delete_messages = can_delete_messages;
        request.can_manage_video_chats = can_manage_video_chats;
        request.can_restrict_members = can_restrict_members;
        request.can_promote_members = can_promote_members;
        request.can_change_info = can_change_info;
        request.can_invite_users = can_invite_users;
        request.can_post_stories = can_post_stories;
        request.can_edit_stories = can_edit_stories;
        request.can_delete_stories = can_delete_stories;
        request.can_post_messages = can_post_messages;
        request.can_edit_messages = can_edit_messages;
        request.can_pin_messages = can_pin_messages;
        request.can_manage_topics = can_manage_topics;
        request.can_manage_direct_messages = can_manage_direct_messages;
        request.can_manage_tags = can_manage_tags;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/promoteChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatAdministratorCustomTitle(std::int64_t chat_id, std::int64_t user_id, const std::string& custom_title) const {
        TgBot::SetChatAdministratorCustomTitleRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.custom_title = custom_title;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatAdministratorCustomTitle";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatAdministratorCustomTitle(const SetChatAdministratorCustomTitleRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.custom_title = custom_title;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatAdministratorCustomTitle";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatMemberTag(std::int64_t chat_id, std::int64_t user_id, const std::string& tag) const {
        TgBot::SetChatMemberTagRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.tag = tag;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatMemberTag";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatMemberTag(const SetChatMemberTagRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        request.tag = tag;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatMemberTag";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::banChatSenderChat(std::int64_t chat_id, std::int64_t sender_chat_id) const {
        TgBot::BanChatSenderChatRequest request{};
        request.chat_id = chat_id;
        request.sender_chat_id = sender_chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/banChatSenderChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::banChatSenderChat(const BanChatSenderChatRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.sender_chat_id = sender_chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/banChatSenderChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unbanChatSenderChat(std::int64_t chat_id, std::int64_t sender_chat_id) const {
        TgBot::UnbanChatSenderChatRequest request{};
        request.chat_id = chat_id;
        request.sender_chat_id = sender_chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unbanChatSenderChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unbanChatSenderChat(const UnbanChatSenderChatRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.sender_chat_id = sender_chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unbanChatSenderChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatPermissions(std::int64_t chat_id, ChatPermissions::Ptr permissions, bool use_independent_chat_permissions) const {
        TgBot::SetChatPermissionsRequest request{};
        request.chat_id = chat_id;
        request.permissions = permissions;
        request.use_independent_chat_permissions = use_independent_chat_permissions;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatPermissions";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatPermissions(const SetChatPermissionsRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.permissions = permissions;
        request.use_independent_chat_permissions = use_independent_chat_permissions;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatPermissions";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<std::string> Api::exportChatInviteLink(std::int64_t chat_id) const {
        TgBot::ExportChatInviteLinkRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/exportChatInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::string>(response);
    }

    coro::task<std::string> Api::exportChatInviteLink(const ExportChatInviteLinkRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/exportChatInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::string>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::createChatInviteLink(std::int64_t chat_id, const std::string& name, std::int64_t expire_date, std::int64_t member_limit, bool creates_join_request) const {
        TgBot::CreateChatInviteLinkRequest request{};
        request.chat_id = chat_id;
        request.name = name;
        request.expire_date = expire_date;
        request.member_limit = member_limit;
        request.creates_join_request = creates_join_request;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createChatInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::createChatInviteLink(const CreateChatInviteLinkRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.name = name;
        request.expire_date = expire_date;
        request.member_limit = member_limit;
        request.creates_join_request = creates_join_request;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createChatInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::editChatInviteLink(std::int64_t chat_id, const std::string& invite_link, const std::string& name, std::int64_t expire_date, std::int64_t member_limit, bool creates_join_request) const {
        TgBot::EditChatInviteLinkRequest request{};
        request.chat_id = chat_id;
        request.invite_link = invite_link;
        request.name = name;
        request.expire_date = expire_date;
        request.member_limit = member_limit;
        request.creates_join_request = creates_join_request;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editChatInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::editChatInviteLink(const EditChatInviteLinkRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.invite_link = invite_link;
        request.name = name;
        request.expire_date = expire_date;
        request.member_limit = member_limit;
        request.creates_join_request = creates_join_request;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editChatInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::createChatSubscriptionInviteLink(std::int64_t chat_id, std::int64_t subscription_period, std::int64_t subscription_price, const std::string& name) const {
        TgBot::CreateChatSubscriptionInviteLinkRequest request{};
        request.chat_id = chat_id;
        request.subscription_period = subscription_period;
        request.subscription_price = subscription_price;
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createChatSubscriptionInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::createChatSubscriptionInviteLink(const CreateChatSubscriptionInviteLinkRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.subscription_period = subscription_period;
        request.subscription_price = subscription_price;
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createChatSubscriptionInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::editChatSubscriptionInviteLink(std::int64_t chat_id, const std::string& invite_link, const std::string& name) const {
        TgBot::EditChatSubscriptionInviteLinkRequest request{};
        request.chat_id = chat_id;
        request.invite_link = invite_link;
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editChatSubscriptionInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::editChatSubscriptionInviteLink(const EditChatSubscriptionInviteLinkRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.invite_link = invite_link;
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editChatSubscriptionInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::revokeChatInviteLink(std::int64_t chat_id, const std::string& invite_link) const {
        TgBot::RevokeChatInviteLinkRequest request{};
        request.chat_id = chat_id;
        request.invite_link = invite_link;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/revokeChatInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<ChatInviteLink::Ptr> Api::revokeChatInviteLink(const RevokeChatInviteLinkRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.invite_link = invite_link;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/revokeChatInviteLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatInviteLink::Ptr>(response);
    }

    coro::task<bool> Api::approveChatJoinRequest(std::int64_t chat_id, std::int64_t user_id) const {
        TgBot::ApproveChatJoinRequestRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/approveChatJoinRequest";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::approveChatJoinRequest(const ApproveChatJoinRequestRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/approveChatJoinRequest";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::declineChatJoinRequest(std::int64_t chat_id, std::int64_t user_id) const {
        TgBot::DeclineChatJoinRequestRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/declineChatJoinRequest";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::declineChatJoinRequest(const DeclineChatJoinRequestRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/declineChatJoinRequest";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatPhoto(std::int64_t chat_id, InputFile::Ptr photo) const {
        TgBot::SetChatPhotoRequest request{};
        request.chat_id = chat_id;
        request.photo = photo;
        // Generate multipart form data for file uploads
        std::string boundary = "----TelegramBotAPI123456789";
        std::string body = toMultipart(request, boundary);
        std::string contentType = "multipart/form-data; boundary=" + boundary;
        const std::string target = "/bot" + api_key_ + "/setChatPhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatPhoto(const SetChatPhotoRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.photo = photo;
        // Generate multipart form data for file uploads
        std::string boundary = "----TelegramBotAPI123456789";
        std::string body = toMultipart(request, boundary);
        std::string contentType = "multipart/form-data; boundary=" + boundary;
        const std::string target = "/bot" + api_key_ + "/setChatPhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteChatPhoto(std::int64_t chat_id) const {
        TgBot::DeleteChatPhotoRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteChatPhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteChatPhoto(const DeleteChatPhotoRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteChatPhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatTitle(std::int64_t chat_id, const std::string& title) const {
        TgBot::SetChatTitleRequest request{};
        request.chat_id = chat_id;
        request.title = title;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatTitle";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatTitle(const SetChatTitleRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.title = title;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatTitle";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatDescription(std::int64_t chat_id, const std::string& description) const {
        TgBot::SetChatDescriptionRequest request{};
        request.chat_id = chat_id;
        request.description = description;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatDescription(const SetChatDescriptionRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.description = description;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::pinChatMessage(std::int64_t chat_id, std::int64_t message_id, const std::string& business_connection_id, bool disable_notification) const {
        TgBot::PinChatMessageRequest request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.business_connection_id = business_connection_id;
        request.disable_notification = disable_notification;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/pinChatMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::pinChatMessage(const PinChatMessageRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.business_connection_id = business_connection_id;
        request.disable_notification = disable_notification;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/pinChatMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unpinChatMessage(std::int64_t chat_id, const std::string& business_connection_id, std::int64_t message_id) const {
        TgBot::UnpinChatMessageRequest request{};
        request.chat_id = chat_id;
        request.business_connection_id = business_connection_id;
        request.message_id = message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unpinChatMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unpinChatMessage(const UnpinChatMessageRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.business_connection_id = business_connection_id;
        request.message_id = message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unpinChatMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unpinAllChatMessages(std::int64_t chat_id) const {
        TgBot::UnpinAllChatMessagesRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unpinAllChatMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unpinAllChatMessages(const UnpinAllChatMessagesRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unpinAllChatMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::leaveChat(std::int64_t chat_id) const {
        TgBot::LeaveChatRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/leaveChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::leaveChat(const LeaveChatRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/leaveChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<ChatFullInfo::Ptr> Api::getChat(std::int64_t chat_id) const {
        TgBot::GetChatRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatFullInfo::Ptr>(response);
    }

    coro::task<ChatFullInfo::Ptr> Api::getChat(const GetChatRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatFullInfo::Ptr>(response);
    }

    coro::task<std::vector<ChatMember::Ptr>> Api::getChatAdministrators(std::int64_t chat_id) const {
        TgBot::GetChatAdministratorsRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatAdministrators";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<ChatMember::Ptr>>(response);
    }

    coro::task<std::vector<ChatMember::Ptr>> Api::getChatAdministrators(const GetChatAdministratorsRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatAdministrators";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<ChatMember::Ptr>>(response);
    }

    coro::task<std::int64_t> Api::getChatMemberCount(std::int64_t chat_id) const {
        TgBot::GetChatMemberCountRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatMemberCount";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::int64_t>(response);
    }

    coro::task<std::int64_t> Api::getChatMemberCount(const GetChatMemberCountRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatMemberCount";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::int64_t>(response);
    }

    coro::task<ChatMember::Ptr> Api::getChatMember(std::int64_t chat_id, std::int64_t user_id) const {
        TgBot::GetChatMemberRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatMember::Ptr>(response);
    }

    coro::task<ChatMember::Ptr> Api::getChatMember(const GetChatMemberRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatMember";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatMember::Ptr>(response);
    }

    coro::task<bool> Api::setChatStickerSet(std::int64_t chat_id, const std::string& sticker_set_name) const {
        TgBot::SetChatStickerSetRequest request{};
        request.chat_id = chat_id;
        request.sticker_set_name = sticker_set_name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatStickerSet(const SetChatStickerSetRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.sticker_set_name = sticker_set_name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteChatStickerSet(std::int64_t chat_id) const {
        TgBot::DeleteChatStickerSetRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteChatStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteChatStickerSet(const DeleteChatStickerSetRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteChatStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<std::vector<Sticker::Ptr>> Api::getForumTopicIconStickers() const {
        const std::string target = "/bot" + api_key_ + "/getForumTopicIconStickers";
        std::string response = co_await http_client_.makeRequest(HttpVerb::GET, target);
        co_return parseResponse<std::vector<Sticker::Ptr>>(response);
    }

    coro::task<ForumTopic::Ptr> Api::createForumTopic(std::int64_t chat_id, const std::string& name, std::int64_t icon_color, const std::string& icon_custom_emoji_id) const {
        TgBot::CreateForumTopicRequest request{};
        request.chat_id = chat_id;
        request.name = name;
        request.icon_color = icon_color;
        request.icon_custom_emoji_id = icon_custom_emoji_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ForumTopic::Ptr>(response);
    }

    coro::task<ForumTopic::Ptr> Api::createForumTopic(const CreateForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.name = name;
        request.icon_color = icon_color;
        request.icon_custom_emoji_id = icon_custom_emoji_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ForumTopic::Ptr>(response);
    }

    coro::task<bool> Api::editForumTopic(std::int64_t chat_id, std::int64_t message_thread_id, const std::string& name, const std::string& icon_custom_emoji_id) const {
        TgBot::EditForumTopicRequest request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        request.name = name;
        request.icon_custom_emoji_id = icon_custom_emoji_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::editForumTopic(const EditForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        request.name = name;
        request.icon_custom_emoji_id = icon_custom_emoji_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::closeForumTopic(std::int64_t chat_id, std::int64_t message_thread_id) const {
        TgBot::CloseForumTopicRequest request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/closeForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::closeForumTopic(const CloseForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/closeForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::reopenForumTopic(std::int64_t chat_id, std::int64_t message_thread_id) const {
        TgBot::ReopenForumTopicRequest request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/reopenForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::reopenForumTopic(const ReopenForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/reopenForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteForumTopic(std::int64_t chat_id, std::int64_t message_thread_id) const {
        TgBot::DeleteForumTopicRequest request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteForumTopic(const DeleteForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unpinAllForumTopicMessages(std::int64_t chat_id, std::int64_t message_thread_id) const {
        TgBot::UnpinAllForumTopicMessagesRequest request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unpinAllForumTopicMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unpinAllForumTopicMessages(const UnpinAllForumTopicMessagesRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_thread_id = message_thread_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unpinAllForumTopicMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::editGeneralForumTopic(std::int64_t chat_id, const std::string& name) const {
        TgBot::EditGeneralForumTopicRequest request{};
        request.chat_id = chat_id;
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::editGeneralForumTopic(const EditGeneralForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::closeGeneralForumTopic(std::int64_t chat_id) const {
        TgBot::CloseGeneralForumTopicRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/closeGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::closeGeneralForumTopic(const CloseGeneralForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/closeGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::reopenGeneralForumTopic(std::int64_t chat_id) const {
        TgBot::ReopenGeneralForumTopicRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/reopenGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::reopenGeneralForumTopic(const ReopenGeneralForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/reopenGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::hideGeneralForumTopic(std::int64_t chat_id) const {
        TgBot::HideGeneralForumTopicRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/hideGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::hideGeneralForumTopic(const HideGeneralForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/hideGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unhideGeneralForumTopic(std::int64_t chat_id) const {
        TgBot::UnhideGeneralForumTopicRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unhideGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unhideGeneralForumTopic(const UnhideGeneralForumTopicRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unhideGeneralForumTopic";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unpinAllGeneralForumTopicMessages(std::int64_t chat_id) const {
        TgBot::UnpinAllGeneralForumTopicMessagesRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unpinAllGeneralForumTopicMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::unpinAllGeneralForumTopicMessages(const UnpinAllGeneralForumTopicMessagesRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/unpinAllGeneralForumTopicMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::answerCallbackQuery(const std::string& callback_query_id, const std::string& text, bool show_alert, const std::string& url, std::int64_t cache_time) const {
        TgBot::AnswerCallbackQueryRequest request{};
        request.callback_query_id = callback_query_id;
        request.text = text;
        request.show_alert = show_alert;
        request.url = url;
        request.cache_time = cache_time;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerCallbackQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::answerCallbackQuery(const AnswerCallbackQueryRequest& request) const {
         request{};
        request.callback_query_id = callback_query_id;
        request.text = text;
        request.show_alert = show_alert;
        request.url = url;
        request.cache_time = cache_time;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerCallbackQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<UserChatBoosts::Ptr> Api::getUserChatBoosts(std::int64_t chat_id, std::int64_t user_id) const {
        TgBot::GetUserChatBoostsRequest request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUserChatBoosts";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<UserChatBoosts::Ptr>(response);
    }

    coro::task<UserChatBoosts::Ptr> Api::getUserChatBoosts(const GetUserChatBoostsRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUserChatBoosts";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<UserChatBoosts::Ptr>(response);
    }

    coro::task<BusinessConnection::Ptr> Api::getBusinessConnection(const std::string& business_connection_id) const {
        TgBot::GetBusinessConnectionRequest request{};
        request.business_connection_id = business_connection_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getBusinessConnection";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<BusinessConnection::Ptr>(response);
    }

    coro::task<BusinessConnection::Ptr> Api::getBusinessConnection(const GetBusinessConnectionRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getBusinessConnection";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<BusinessConnection::Ptr>(response);
    }

    coro::task<bool> Api::setMyCommands(const std::vector<BotCommand::Ptr>& commands, BotCommandScope::Ptr scope, const std::string& language_code) const {
        TgBot::SetMyCommandsRequest request{};
        request.commands = commands;
        request.scope = scope;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyCommands";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setMyCommands(const SetMyCommandsRequest& request) const {
         request{};
        request.commands = commands;
        request.scope = scope;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyCommands";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteMyCommands(BotCommandScope::Ptr scope, const std::string& language_code) const {
        TgBot::DeleteMyCommandsRequest request{};
        request.scope = scope;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteMyCommands";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteMyCommands(const DeleteMyCommandsRequest& request) const {
         request{};
        request.scope = scope;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteMyCommands";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<std::vector<BotCommand::Ptr>> Api::getMyCommands(BotCommandScope::Ptr scope, const std::string& language_code) const {
        TgBot::GetMyCommandsRequest request{};
        request.scope = scope;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyCommands";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<BotCommand::Ptr>>(response);
    }

    coro::task<std::vector<BotCommand::Ptr>> Api::getMyCommands(const GetMyCommandsRequest& request) const {
         request{};
        request.scope = scope;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyCommands";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<BotCommand::Ptr>>(response);
    }

    coro::task<bool> Api::setMyName(const std::string& name, const std::string& language_code) const {
        TgBot::SetMyNameRequest request{};
        request.name = name;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyName";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setMyName(const SetMyNameRequest& request) const {
         request{};
        request.name = name;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyName";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<BotName::Ptr> Api::getMyName(const std::string& language_code) const {
        TgBot::GetMyNameRequest request{};
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyName";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<BotName::Ptr>(response);
    }

    coro::task<BotName::Ptr> Api::getMyName(const GetMyNameRequest& request) const {
         request{};
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyName";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<BotName::Ptr>(response);
    }

    coro::task<bool> Api::setMyDescription(const std::string& description, const std::string& language_code) const {
        TgBot::SetMyDescriptionRequest request{};
        request.description = description;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setMyDescription(const SetMyDescriptionRequest& request) const {
         request{};
        request.description = description;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<BotDescription::Ptr> Api::getMyDescription(const std::string& language_code) const {
        TgBot::GetMyDescriptionRequest request{};
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<BotDescription::Ptr>(response);
    }

    coro::task<BotDescription::Ptr> Api::getMyDescription(const GetMyDescriptionRequest& request) const {
         request{};
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<BotDescription::Ptr>(response);
    }

    coro::task<bool> Api::setMyShortDescription(const std::string& short_description, const std::string& language_code) const {
        TgBot::SetMyShortDescriptionRequest request{};
        request.short_description = short_description;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyShortDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setMyShortDescription(const SetMyShortDescriptionRequest& request) const {
         request{};
        request.short_description = short_description;
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyShortDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<BotShortDescription::Ptr> Api::getMyShortDescription(const std::string& language_code) const {
        TgBot::GetMyShortDescriptionRequest request{};
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyShortDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<BotShortDescription::Ptr>(response);
    }

    coro::task<BotShortDescription::Ptr> Api::getMyShortDescription(const GetMyShortDescriptionRequest& request) const {
         request{};
        request.language_code = language_code;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyShortDescription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<BotShortDescription::Ptr>(response);
    }

    coro::task<bool> Api::setMyProfilePhoto(InputProfilePhoto::Ptr photo) const {
        TgBot::SetMyProfilePhotoRequest request{};
        request.photo = photo;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyProfilePhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setMyProfilePhoto(const SetMyProfilePhotoRequest& request) const {
         request{};
        request.photo = photo;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyProfilePhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatMenuButton(std::int64_t chat_id, MenuButton::Ptr menu_button) const {
        TgBot::SetChatMenuButtonRequest request{};
        request.chat_id = chat_id;
        request.menu_button = menu_button;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatMenuButton";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setChatMenuButton(const SetChatMenuButtonRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.menu_button = menu_button;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setChatMenuButton";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<MenuButton::Ptr> Api::getChatMenuButton(std::int64_t chat_id) const {
        TgBot::GetChatMenuButtonRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatMenuButton";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<MenuButton::Ptr>(response);
    }

    coro::task<MenuButton::Ptr> Api::getChatMenuButton(const GetChatMenuButtonRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatMenuButton";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<MenuButton::Ptr>(response);
    }

    coro::task<bool> Api::setMyDefaultAdministratorRights(ChatAdministratorRights::Ptr rights, bool for_channels) const {
        TgBot::SetMyDefaultAdministratorRightsRequest request{};
        request.rights = rights;
        request.for_channels = for_channels;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyDefaultAdministratorRights";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setMyDefaultAdministratorRights(const SetMyDefaultAdministratorRightsRequest& request) const {
         request{};
        request.rights = rights;
        request.for_channels = for_channels;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setMyDefaultAdministratorRights";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<ChatAdministratorRights::Ptr> Api::getMyDefaultAdministratorRights(bool for_channels) const {
        TgBot::GetMyDefaultAdministratorRightsRequest request{};
        request.for_channels = for_channels;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyDefaultAdministratorRights";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatAdministratorRights::Ptr>(response);
    }

    coro::task<ChatAdministratorRights::Ptr> Api::getMyDefaultAdministratorRights(const GetMyDefaultAdministratorRightsRequest& request) const {
         request{};
        request.for_channels = for_channels;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getMyDefaultAdministratorRights";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<ChatAdministratorRights::Ptr>(response);
    }

    coro::task<bool> Api::sendGift(const std::string& gift_id, std::int64_t user_id, std::int64_t chat_id, bool pay_for_upgrade, const std::string& text, const std::string& text_parse_mode, const std::vector<MessageEntity::Ptr>& text_entities) const {
        TgBot::SendGiftRequest request{};
        request.gift_id = gift_id;
        request.user_id = user_id;
        request.chat_id = chat_id;
        request.pay_for_upgrade = pay_for_upgrade;
        request.text = text;
        request.text_parse_mode = text_parse_mode;
        request.text_entities = text_entities;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendGift";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::sendGift(const SendGiftRequest& request) const {
         request{};
        request.gift_id = gift_id;
        request.user_id = user_id;
        request.chat_id = chat_id;
        request.pay_for_upgrade = pay_for_upgrade;
        request.text = text;
        request.text_parse_mode = text_parse_mode;
        request.text_entities = text_entities;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendGift";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::giftPremiumSubscription(std::int64_t user_id, std::int64_t month_count, std::int64_t star_count, const std::string& text, const std::string& text_parse_mode, const std::vector<MessageEntity::Ptr>& text_entities) const {
        TgBot::GiftPremiumSubscriptionRequest request{};
        request.user_id = user_id;
        request.month_count = month_count;
        request.star_count = star_count;
        request.text = text;
        request.text_parse_mode = text_parse_mode;
        request.text_entities = text_entities;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/giftPremiumSubscription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::giftPremiumSubscription(const GiftPremiumSubscriptionRequest& request) const {
         request{};
        request.user_id = user_id;
        request.month_count = month_count;
        request.star_count = star_count;
        request.text = text;
        request.text_parse_mode = text_parse_mode;
        request.text_entities = text_entities;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/giftPremiumSubscription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::verifyUser(std::int64_t user_id, const std::string& custom_description) const {
        TgBot::VerifyUserRequest request{};
        request.user_id = user_id;
        request.custom_description = custom_description;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/verifyUser";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::verifyUser(const VerifyUserRequest& request) const {
         request{};
        request.user_id = user_id;
        request.custom_description = custom_description;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/verifyUser";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::verifyChat(std::int64_t chat_id, const std::string& custom_description) const {
        TgBot::VerifyChatRequest request{};
        request.chat_id = chat_id;
        request.custom_description = custom_description;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/verifyChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::verifyChat(const VerifyChatRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.custom_description = custom_description;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/verifyChat";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::removeUserVerification(std::int64_t user_id) const {
        TgBot::RemoveUserVerificationRequest request{};
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/removeUserVerification";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::removeUserVerification(const RemoveUserVerificationRequest& request) const {
         request{};
        request.user_id = user_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/removeUserVerification";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::removeChatVerification(std::int64_t chat_id) const {
        TgBot::RemoveChatVerificationRequest request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/removeChatVerification";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::removeChatVerification(const RemoveChatVerificationRequest& request) const {
         request{};
        request.chat_id = chat_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/removeChatVerification";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::readBusinessMessage(const std::string& business_connection_id, std::int64_t chat_id, std::int64_t message_id) const {
        TgBot::ReadBusinessMessageRequest request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/readBusinessMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::readBusinessMessage(const ReadBusinessMessageRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/readBusinessMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteBusinessMessages(const std::string& business_connection_id, const std::vector<std::int64_t>& message_ids) const {
        TgBot::DeleteBusinessMessagesRequest request{};
        request.business_connection_id = business_connection_id;
        request.message_ids = message_ids;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteBusinessMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteBusinessMessages(const DeleteBusinessMessagesRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.message_ids = message_ids;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteBusinessMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountName(const std::string& business_connection_id, const std::string& first_name, const std::string& last_name) const {
        TgBot::SetBusinessAccountNameRequest request{};
        request.business_connection_id = business_connection_id;
        request.first_name = first_name;
        request.last_name = last_name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountName";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountName(const SetBusinessAccountNameRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.first_name = first_name;
        request.last_name = last_name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountName";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountUsername(const std::string& business_connection_id, const std::string& username) const {
        TgBot::SetBusinessAccountUsernameRequest request{};
        request.business_connection_id = business_connection_id;
        request.username = username;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountUsername";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountUsername(const SetBusinessAccountUsernameRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.username = username;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountUsername";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountBio(const std::string& business_connection_id, const std::string& bio) const {
        TgBot::SetBusinessAccountBioRequest request{};
        request.business_connection_id = business_connection_id;
        request.bio = bio;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountBio";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountBio(const SetBusinessAccountBioRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.bio = bio;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountBio";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountProfilePhoto(const std::string& business_connection_id, InputProfilePhoto::Ptr photo, bool is_public) const {
        TgBot::SetBusinessAccountProfilePhotoRequest request{};
        request.business_connection_id = business_connection_id;
        request.photo = photo;
        request.is_public = is_public;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountProfilePhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountProfilePhoto(const SetBusinessAccountProfilePhotoRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.photo = photo;
        request.is_public = is_public;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountProfilePhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::removeBusinessAccountProfilePhoto(const std::string& business_connection_id, bool is_public) const {
        TgBot::RemoveBusinessAccountProfilePhotoRequest request{};
        request.business_connection_id = business_connection_id;
        request.is_public = is_public;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/removeBusinessAccountProfilePhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::removeBusinessAccountProfilePhoto(const RemoveBusinessAccountProfilePhotoRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.is_public = is_public;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/removeBusinessAccountProfilePhoto";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountGiftSettings(const std::string& business_connection_id, bool show_gift_button, AcceptedGiftTypes::Ptr accepted_gift_types) const {
        TgBot::SetBusinessAccountGiftSettingsRequest request{};
        request.business_connection_id = business_connection_id;
        request.show_gift_button = show_gift_button;
        request.accepted_gift_types = accepted_gift_types;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountGiftSettings";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setBusinessAccountGiftSettings(const SetBusinessAccountGiftSettingsRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.show_gift_button = show_gift_button;
        request.accepted_gift_types = accepted_gift_types;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setBusinessAccountGiftSettings";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<StarAmount::Ptr> Api::getBusinessAccountStarBalance(const std::string& business_connection_id) const {
        TgBot::GetBusinessAccountStarBalanceRequest request{};
        request.business_connection_id = business_connection_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getBusinessAccountStarBalance";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<StarAmount::Ptr>(response);
    }

    coro::task<StarAmount::Ptr> Api::getBusinessAccountStarBalance(const GetBusinessAccountStarBalanceRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getBusinessAccountStarBalance";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<StarAmount::Ptr>(response);
    }

    coro::task<bool> Api::transferBusinessAccountStars(const std::string& business_connection_id, std::int64_t star_count) const {
        TgBot::TransferBusinessAccountStarsRequest request{};
        request.business_connection_id = business_connection_id;
        request.star_count = star_count;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/transferBusinessAccountStars";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::transferBusinessAccountStars(const TransferBusinessAccountStarsRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.star_count = star_count;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/transferBusinessAccountStars";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<OwnedGifts::Ptr> Api::getBusinessAccountGifts(const std::string& business_connection_id, bool exclude_unsaved, bool exclude_saved, bool exclude_unlimited, bool exclude_limited_upgradable, bool exclude_limited_non_upgradable, bool exclude_unique, bool exclude_from_blockchain, bool sort_by_price, const std::string& offset, std::int64_t limit) const {
        TgBot::GetBusinessAccountGiftsRequest request{};
        request.business_connection_id = business_connection_id;
        request.exclude_unsaved = exclude_unsaved;
        request.exclude_saved = exclude_saved;
        request.exclude_unlimited = exclude_unlimited;
        request.exclude_limited_upgradable = exclude_limited_upgradable;
        request.exclude_limited_non_upgradable = exclude_limited_non_upgradable;
        request.exclude_unique = exclude_unique;
        request.exclude_from_blockchain = exclude_from_blockchain;
        request.sort_by_price = sort_by_price;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getBusinessAccountGifts";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<OwnedGifts::Ptr>(response);
    }

    coro::task<OwnedGifts::Ptr> Api::getBusinessAccountGifts(const GetBusinessAccountGiftsRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.exclude_unsaved = exclude_unsaved;
        request.exclude_saved = exclude_saved;
        request.exclude_unlimited = exclude_unlimited;
        request.exclude_limited_upgradable = exclude_limited_upgradable;
        request.exclude_limited_non_upgradable = exclude_limited_non_upgradable;
        request.exclude_unique = exclude_unique;
        request.exclude_from_blockchain = exclude_from_blockchain;
        request.sort_by_price = sort_by_price;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getBusinessAccountGifts";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<OwnedGifts::Ptr>(response);
    }

    coro::task<OwnedGifts::Ptr> Api::getUserGifts(std::int64_t user_id, bool exclude_unlimited, bool exclude_limited_upgradable, bool exclude_limited_non_upgradable, bool exclude_from_blockchain, bool exclude_unique, bool sort_by_price, const std::string& offset, std::int64_t limit) const {
        TgBot::GetUserGiftsRequest request{};
        request.user_id = user_id;
        request.exclude_unlimited = exclude_unlimited;
        request.exclude_limited_upgradable = exclude_limited_upgradable;
        request.exclude_limited_non_upgradable = exclude_limited_non_upgradable;
        request.exclude_from_blockchain = exclude_from_blockchain;
        request.exclude_unique = exclude_unique;
        request.sort_by_price = sort_by_price;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUserGifts";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<OwnedGifts::Ptr>(response);
    }

    coro::task<OwnedGifts::Ptr> Api::getUserGifts(const GetUserGiftsRequest& request) const {
         request{};
        request.user_id = user_id;
        request.exclude_unlimited = exclude_unlimited;
        request.exclude_limited_upgradable = exclude_limited_upgradable;
        request.exclude_limited_non_upgradable = exclude_limited_non_upgradable;
        request.exclude_from_blockchain = exclude_from_blockchain;
        request.exclude_unique = exclude_unique;
        request.sort_by_price = sort_by_price;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getUserGifts";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<OwnedGifts::Ptr>(response);
    }

    coro::task<OwnedGifts::Ptr> Api::getChatGifts(std::int64_t chat_id, bool exclude_unsaved, bool exclude_saved, bool exclude_unlimited, bool exclude_limited_upgradable, bool exclude_limited_non_upgradable, bool exclude_from_blockchain, bool exclude_unique, bool sort_by_price, const std::string& offset, std::int64_t limit) const {
        TgBot::GetChatGiftsRequest request{};
        request.chat_id = chat_id;
        request.exclude_unsaved = exclude_unsaved;
        request.exclude_saved = exclude_saved;
        request.exclude_unlimited = exclude_unlimited;
        request.exclude_limited_upgradable = exclude_limited_upgradable;
        request.exclude_limited_non_upgradable = exclude_limited_non_upgradable;
        request.exclude_from_blockchain = exclude_from_blockchain;
        request.exclude_unique = exclude_unique;
        request.sort_by_price = sort_by_price;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatGifts";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<OwnedGifts::Ptr>(response);
    }

    coro::task<OwnedGifts::Ptr> Api::getChatGifts(const GetChatGiftsRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.exclude_unsaved = exclude_unsaved;
        request.exclude_saved = exclude_saved;
        request.exclude_unlimited = exclude_unlimited;
        request.exclude_limited_upgradable = exclude_limited_upgradable;
        request.exclude_limited_non_upgradable = exclude_limited_non_upgradable;
        request.exclude_from_blockchain = exclude_from_blockchain;
        request.exclude_unique = exclude_unique;
        request.sort_by_price = sort_by_price;
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getChatGifts";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<OwnedGifts::Ptr>(response);
    }

    coro::task<bool> Api::convertGiftToStars(const std::string& business_connection_id, const std::string& owned_gift_id) const {
        TgBot::ConvertGiftToStarsRequest request{};
        request.business_connection_id = business_connection_id;
        request.owned_gift_id = owned_gift_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/convertGiftToStars";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::convertGiftToStars(const ConvertGiftToStarsRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.owned_gift_id = owned_gift_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/convertGiftToStars";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::upgradeGift(const std::string& business_connection_id, const std::string& owned_gift_id, bool keep_original_details, std::int64_t star_count) const {
        TgBot::UpgradeGiftRequest request{};
        request.business_connection_id = business_connection_id;
        request.owned_gift_id = owned_gift_id;
        request.keep_original_details = keep_original_details;
        request.star_count = star_count;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/upgradeGift";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::upgradeGift(const UpgradeGiftRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.owned_gift_id = owned_gift_id;
        request.keep_original_details = keep_original_details;
        request.star_count = star_count;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/upgradeGift";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::transferGift(const std::string& business_connection_id, const std::string& owned_gift_id, std::int64_t new_owner_chat_id, std::int64_t star_count) const {
        TgBot::TransferGiftRequest request{};
        request.business_connection_id = business_connection_id;
        request.owned_gift_id = owned_gift_id;
        request.new_owner_chat_id = new_owner_chat_id;
        request.star_count = star_count;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/transferGift";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::transferGift(const TransferGiftRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.owned_gift_id = owned_gift_id;
        request.new_owner_chat_id = new_owner_chat_id;
        request.star_count = star_count;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/transferGift";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<Story::Ptr> Api::postStory(const std::string& business_connection_id, InputStoryContent::Ptr content, std::int64_t active_period, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, const std::vector<StoryArea::Ptr>& areas, bool post_to_chat_page, bool protect_content) const {
        TgBot::PostStoryRequest request{};
        request.business_connection_id = business_connection_id;
        request.content = content;
        request.active_period = active_period;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.areas = areas;
        request.post_to_chat_page = post_to_chat_page;
        request.protect_content = protect_content;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/postStory";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Story::Ptr>(response);
    }

    coro::task<Story::Ptr> Api::postStory(const PostStoryRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.content = content;
        request.active_period = active_period;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.areas = areas;
        request.post_to_chat_page = post_to_chat_page;
        request.protect_content = protect_content;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/postStory";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Story::Ptr>(response);
    }

    coro::task<Story::Ptr> Api::repostStory(const std::string& business_connection_id, std::int64_t from_chat_id, std::int64_t from_story_id, std::int64_t active_period, bool post_to_chat_page, bool protect_content) const {
        TgBot::RepostStoryRequest request{};
        request.business_connection_id = business_connection_id;
        request.from_chat_id = from_chat_id;
        request.from_story_id = from_story_id;
        request.active_period = active_period;
        request.post_to_chat_page = post_to_chat_page;
        request.protect_content = protect_content;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/repostStory";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Story::Ptr>(response);
    }

    coro::task<Story::Ptr> Api::repostStory(const RepostStoryRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.from_chat_id = from_chat_id;
        request.from_story_id = from_story_id;
        request.active_period = active_period;
        request.post_to_chat_page = post_to_chat_page;
        request.protect_content = protect_content;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/repostStory";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Story::Ptr>(response);
    }

    coro::task<Story::Ptr> Api::editStory(const std::string& business_connection_id, std::int64_t story_id, InputStoryContent::Ptr content, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, const std::vector<StoryArea::Ptr>& areas) const {
        TgBot::EditStoryRequest request{};
        request.business_connection_id = business_connection_id;
        request.story_id = story_id;
        request.content = content;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.areas = areas;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editStory";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Story::Ptr>(response);
    }

    coro::task<Story::Ptr> Api::editStory(const EditStoryRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.story_id = story_id;
        request.content = content;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.areas = areas;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editStory";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Story::Ptr>(response);
    }

    coro::task<bool> Api::deleteStory(const std::string& business_connection_id, std::int64_t story_id) const {
        TgBot::DeleteStoryRequest request{};
        request.business_connection_id = business_connection_id;
        request.story_id = story_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteStory";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteStory(const DeleteStoryRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.story_id = story_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteStory";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }


    // Updating messages

    coro::task<Message::Ptr> Api::editMessageText(const std::string& text, const std::string& business_connection_id, std::int64_t chat_id, std::int64_t message_id, const std::string& inline_message_id, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& entities, LinkPreviewOptions::Ptr link_preview_options, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::EditMessageTextRequest request{};
        request.text = text;
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.parse_mode = parse_mode;
        request.entities = entities;
        request.link_preview_options = link_preview_options;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageText";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageText(const EditMessageTextRequest& request) const {
         request{};
        request.text = text;
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.parse_mode = parse_mode;
        request.entities = entities;
        request.link_preview_options = link_preview_options;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageText";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageCaption(const std::string& business_connection_id, std::int64_t chat_id, std::int64_t message_id, const std::string& inline_message_id, const std::string& caption, const std::string& parse_mode, const std::vector<MessageEntity::Ptr>& caption_entities, bool show_caption_above_media, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::EditMessageCaptionRequest request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageCaption";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageCaption(const EditMessageCaptionRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.caption = caption;
        request.parse_mode = parse_mode;
        request.caption_entities = caption_entities;
        request.show_caption_above_media = show_caption_above_media;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageCaption";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageMedia(InputMedia::Ptr media, const std::string& business_connection_id, std::int64_t chat_id, std::int64_t message_id, const std::string& inline_message_id, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::EditMessageMediaRequest request{};
        request.media = media;
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageMedia";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageMedia(const EditMessageMediaRequest& request) const {
         request{};
        request.media = media;
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageMedia";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageLiveLocation(double latitude, double longitude, const std::string& business_connection_id, std::int64_t chat_id, std::int64_t message_id, const std::string& inline_message_id, std::int64_t live_period, double horizontal_accuracy, std::int64_t heading, std::int64_t proximity_alert_radius, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::EditMessageLiveLocationRequest request{};
        request.latitude = latitude;
        request.longitude = longitude;
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.live_period = live_period;
        request.horizontal_accuracy = horizontal_accuracy;
        request.heading = heading;
        request.proximity_alert_radius = proximity_alert_radius;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageLiveLocation";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageLiveLocation(const EditMessageLiveLocationRequest& request) const {
         request{};
        request.latitude = latitude;
        request.longitude = longitude;
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.live_period = live_period;
        request.horizontal_accuracy = horizontal_accuracy;
        request.heading = heading;
        request.proximity_alert_radius = proximity_alert_radius;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageLiveLocation";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::stopMessageLiveLocation(const std::string& business_connection_id, std::int64_t chat_id, std::int64_t message_id, const std::string& inline_message_id, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::StopMessageLiveLocationRequest request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/stopMessageLiveLocation";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::stopMessageLiveLocation(const StopMessageLiveLocationRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/stopMessageLiveLocation";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageChecklist(const std::string& business_connection_id, std::int64_t chat_id, std::int64_t message_id, InputChecklist::Ptr checklist, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::EditMessageChecklistRequest request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.checklist = checklist;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageChecklist";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageChecklist(const EditMessageChecklistRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.checklist = checklist;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageChecklist";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageReplyMarkup(const std::string& business_connection_id, std::int64_t chat_id, std::int64_t message_id, const std::string& inline_message_id, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::EditMessageReplyMarkupRequest request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageReplyMarkup";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::editMessageReplyMarkup(const EditMessageReplyMarkupRequest& request) const {
         request{};
        request.business_connection_id = business_connection_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editMessageReplyMarkup";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Poll::Ptr> Api::stopPoll(std::int64_t chat_id, std::int64_t message_id, const std::string& business_connection_id, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::StopPollRequest request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.business_connection_id = business_connection_id;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/stopPoll";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Poll::Ptr>(response);
    }

    coro::task<Poll::Ptr> Api::stopPoll(const StopPollRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.business_connection_id = business_connection_id;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/stopPoll";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Poll::Ptr>(response);
    }

    coro::task<bool> Api::approveSuggestedPost(std::int64_t chat_id, std::int64_t message_id, std::int64_t send_date) const {
        TgBot::ApproveSuggestedPostRequest request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.send_date = send_date;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/approveSuggestedPost";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::approveSuggestedPost(const ApproveSuggestedPostRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.send_date = send_date;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/approveSuggestedPost";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::declineSuggestedPost(std::int64_t chat_id, std::int64_t message_id, const std::string& comment) const {
        TgBot::DeclineSuggestedPostRequest request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.comment = comment;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/declineSuggestedPost";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::declineSuggestedPost(const DeclineSuggestedPostRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.comment = comment;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/declineSuggestedPost";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteMessage(std::int64_t chat_id, std::int64_t message_id) const {
        TgBot::DeleteMessageRequest request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteMessage(const DeleteMessageRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_id = message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteMessages(std::int64_t chat_id, const std::vector<std::int64_t>& message_ids) const {
        TgBot::DeleteMessagesRequest request{};
        request.chat_id = chat_id;
        request.message_ids = message_ids;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteMessages(const DeleteMessagesRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.message_ids = message_ids;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteMessages";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }


    // Stickers

    coro::task<Message::Ptr> Api::sendSticker(std::int64_t chat_id, const std::string& sticker, const std::string& business_connection_id, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& emoji, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, KeyboardOption::Ptr reply_markup) const {
        TgBot::SendStickerRequest request{};
        request.chat_id = chat_id;
        request.sticker = sticker;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.emoji = emoji;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendSticker";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendSticker(const SendStickerRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.sticker = sticker;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.emoji = emoji;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendSticker";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<StickerSet::Ptr> Api::getStickerSet(const std::string& name) const {
        TgBot::GetStickerSetRequest request{};
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<StickerSet::Ptr>(response);
    }

    coro::task<StickerSet::Ptr> Api::getStickerSet(const GetStickerSetRequest& request) const {
         request{};
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<StickerSet::Ptr>(response);
    }

    coro::task<std::vector<Sticker::Ptr>> Api::getCustomEmojiStickers(const std::vector<std::string>& custom_emoji_ids) const {
        TgBot::GetCustomEmojiStickersRequest request{};
        request.custom_emoji_ids = custom_emoji_ids;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getCustomEmojiStickers";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<Sticker::Ptr>>(response);
    }

    coro::task<std::vector<Sticker::Ptr>> Api::getCustomEmojiStickers(const GetCustomEmojiStickersRequest& request) const {
         request{};
        request.custom_emoji_ids = custom_emoji_ids;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getCustomEmojiStickers";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<Sticker::Ptr>>(response);
    }

    coro::task<File::Ptr> Api::uploadStickerFile(std::int64_t user_id, InputFile::Ptr sticker, const std::string& sticker_format) const {
        TgBot::UploadStickerFileRequest request{};
        request.user_id = user_id;
        request.sticker = sticker;
        request.sticker_format = sticker_format;
        // Generate multipart form data for file uploads
        std::string boundary = "----TelegramBotAPI123456789";
        std::string body = toMultipart(request, boundary);
        std::string contentType = "multipart/form-data; boundary=" + boundary;
        const std::string target = "/bot" + api_key_ + "/uploadStickerFile";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<File::Ptr>(response);
    }

    coro::task<File::Ptr> Api::uploadStickerFile(const UploadStickerFileRequest& request) const {
         request{};
        request.user_id = user_id;
        request.sticker = sticker;
        request.sticker_format = sticker_format;
        // Generate multipart form data for file uploads
        std::string boundary = "----TelegramBotAPI123456789";
        std::string body = toMultipart(request, boundary);
        std::string contentType = "multipart/form-data; boundary=" + boundary;
        const std::string target = "/bot" + api_key_ + "/uploadStickerFile";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<File::Ptr>(response);
    }

    coro::task<bool> Api::createNewStickerSet(std::int64_t user_id, const std::string& name, const std::string& title, const std::vector<InputSticker::Ptr>& stickers, const std::string& sticker_type, bool needs_repainting) const {
        TgBot::CreateNewStickerSetRequest request{};
        request.user_id = user_id;
        request.name = name;
        request.title = title;
        request.stickers = stickers;
        request.sticker_type = sticker_type;
        request.needs_repainting = needs_repainting;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createNewStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::createNewStickerSet(const CreateNewStickerSetRequest& request) const {
         request{};
        request.user_id = user_id;
        request.name = name;
        request.title = title;
        request.stickers = stickers;
        request.sticker_type = sticker_type;
        request.needs_repainting = needs_repainting;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createNewStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::addStickerToSet(std::int64_t user_id, const std::string& name, InputSticker::Ptr sticker) const {
        TgBot::AddStickerToSetRequest request{};
        request.user_id = user_id;
        request.name = name;
        request.sticker = sticker;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/addStickerToSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::addStickerToSet(const AddStickerToSetRequest& request) const {
         request{};
        request.user_id = user_id;
        request.name = name;
        request.sticker = sticker;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/addStickerToSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerPositionInSet(const std::string& sticker, std::int64_t position) const {
        TgBot::SetStickerPositionInSetRequest request{};
        request.sticker = sticker;
        request.position = position;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerPositionInSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerPositionInSet(const SetStickerPositionInSetRequest& request) const {
         request{};
        request.sticker = sticker;
        request.position = position;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerPositionInSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteStickerFromSet(const std::string& sticker) const {
        TgBot::DeleteStickerFromSetRequest request{};
        request.sticker = sticker;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteStickerFromSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteStickerFromSet(const DeleteStickerFromSetRequest& request) const {
         request{};
        request.sticker = sticker;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteStickerFromSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::replaceStickerInSet(std::int64_t user_id, const std::string& name, const std::string& old_sticker, InputSticker::Ptr sticker) const {
        TgBot::ReplaceStickerInSetRequest request{};
        request.user_id = user_id;
        request.name = name;
        request.old_sticker = old_sticker;
        request.sticker = sticker;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/replaceStickerInSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::replaceStickerInSet(const ReplaceStickerInSetRequest& request) const {
         request{};
        request.user_id = user_id;
        request.name = name;
        request.old_sticker = old_sticker;
        request.sticker = sticker;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/replaceStickerInSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerEmojiList(const std::string& sticker, const std::vector<std::string>& emoji_list) const {
        TgBot::SetStickerEmojiListRequest request{};
        request.sticker = sticker;
        request.emoji_list = emoji_list;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerEmojiList";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerEmojiList(const SetStickerEmojiListRequest& request) const {
         request{};
        request.sticker = sticker;
        request.emoji_list = emoji_list;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerEmojiList";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerKeywords(const std::string& sticker, const std::vector<std::string>& keywords) const {
        TgBot::SetStickerKeywordsRequest request{};
        request.sticker = sticker;
        request.keywords = keywords;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerKeywords";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerKeywords(const SetStickerKeywordsRequest& request) const {
         request{};
        request.sticker = sticker;
        request.keywords = keywords;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerKeywords";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerMaskPosition(const std::string& sticker, MaskPosition::Ptr mask_position) const {
        TgBot::SetStickerMaskPositionRequest request{};
        request.sticker = sticker;
        request.mask_position = mask_position;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerMaskPosition";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerMaskPosition(const SetStickerMaskPositionRequest& request) const {
         request{};
        request.sticker = sticker;
        request.mask_position = mask_position;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerMaskPosition";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerSetTitle(const std::string& name, const std::string& title) const {
        TgBot::SetStickerSetTitleRequest request{};
        request.name = name;
        request.title = title;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerSetTitle";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerSetTitle(const SetStickerSetTitleRequest& request) const {
         request{};
        request.name = name;
        request.title = title;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerSetTitle";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerSetThumbnail(const std::string& name, std::int64_t user_id, const std::string& format, const std::string& thumbnail) const {
        TgBot::SetStickerSetThumbnailRequest request{};
        request.name = name;
        request.user_id = user_id;
        request.format = format;
        request.thumbnail = thumbnail;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerSetThumbnail";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setStickerSetThumbnail(const SetStickerSetThumbnailRequest& request) const {
         request{};
        request.name = name;
        request.user_id = user_id;
        request.format = format;
        request.thumbnail = thumbnail;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setStickerSetThumbnail";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setCustomEmojiStickerSetThumbnail(const std::string& name, const std::string& custom_emoji_id) const {
        TgBot::SetCustomEmojiStickerSetThumbnailRequest request{};
        request.name = name;
        request.custom_emoji_id = custom_emoji_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setCustomEmojiStickerSetThumbnail";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setCustomEmojiStickerSetThumbnail(const SetCustomEmojiStickerSetThumbnailRequest& request) const {
         request{};
        request.name = name;
        request.custom_emoji_id = custom_emoji_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setCustomEmojiStickerSetThumbnail";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteStickerSet(const std::string& name) const {
        TgBot::DeleteStickerSetRequest request{};
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::deleteStickerSet(const DeleteStickerSetRequest& request) const {
         request{};
        request.name = name;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/deleteStickerSet";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }


    // Inline mode

    coro::task<bool> Api::answerInlineQuery(const std::string& inline_query_id, const std::vector<InlineQueryResult::Ptr>& results, std::int64_t cache_time, bool is_personal, const std::string& next_offset, InlineQueryResultsButton::Ptr button) const {
        TgBot::AnswerInlineQueryRequest request{};
        request.inline_query_id = inline_query_id;
        request.results = results;
        request.cache_time = cache_time;
        request.is_personal = is_personal;
        request.next_offset = next_offset;
        request.button = button;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerInlineQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::answerInlineQuery(const AnswerInlineQueryRequest& request) const {
         request{};
        request.inline_query_id = inline_query_id;
        request.results = results;
        request.cache_time = cache_time;
        request.is_personal = is_personal;
        request.next_offset = next_offset;
        request.button = button;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerInlineQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<SentWebAppMessage::Ptr> Api::answerWebAppQuery(const std::string& web_app_query_id, InlineQueryResult::Ptr result) const {
        TgBot::AnswerWebAppQueryRequest request{};
        request.web_app_query_id = web_app_query_id;
        request.result = result;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerWebAppQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<SentWebAppMessage::Ptr>(response);
    }

    coro::task<SentWebAppMessage::Ptr> Api::answerWebAppQuery(const AnswerWebAppQueryRequest& request) const {
         request{};
        request.web_app_query_id = web_app_query_id;
        request.result = result;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerWebAppQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<SentWebAppMessage::Ptr>(response);
    }

    coro::task<PreparedInlineMessage::Ptr> Api::savePreparedInlineMessage(std::int64_t user_id, InlineQueryResult::Ptr result, bool allow_user_chats, bool allow_bot_chats, bool allow_group_chats, bool allow_channel_chats) const {
        TgBot::SavePreparedInlineMessageRequest request{};
        request.user_id = user_id;
        request.result = result;
        request.allow_user_chats = allow_user_chats;
        request.allow_bot_chats = allow_bot_chats;
        request.allow_group_chats = allow_group_chats;
        request.allow_channel_chats = allow_channel_chats;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/savePreparedInlineMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<PreparedInlineMessage::Ptr>(response);
    }

    coro::task<PreparedInlineMessage::Ptr> Api::savePreparedInlineMessage(const SavePreparedInlineMessageRequest& request) const {
         request{};
        request.user_id = user_id;
        request.result = result;
        request.allow_user_chats = allow_user_chats;
        request.allow_bot_chats = allow_bot_chats;
        request.allow_group_chats = allow_group_chats;
        request.allow_channel_chats = allow_channel_chats;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/savePreparedInlineMessage";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<PreparedInlineMessage::Ptr>(response);
    }


    // Payments

    coro::task<Message::Ptr> Api::sendInvoice(std::int64_t chat_id, const std::string& title, const std::string& description, const std::string& payload, const std::string& currency, const std::vector<LabeledPrice::Ptr>& prices, std::int64_t message_thread_id, std::int64_t direct_messages_topic_id, const std::string& provider_token, std::int64_t max_tip_amount, const std::vector<std::int64_t>& suggested_tip_amounts, const std::string& start_parameter, const std::string& provider_data, const std::string& photo_url, std::int64_t photo_size, std::int64_t photo_width, std::int64_t photo_height, bool need_name, bool need_phone_number, bool need_email, bool need_shipping_address, bool send_phone_number_to_provider, bool send_email_to_provider, bool is_flexible, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, SuggestedPostParameters::Ptr suggested_post_parameters, ReplyParameters::Ptr reply_parameters, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::SendInvoiceRequest request{};
        request.chat_id = chat_id;
        request.title = title;
        request.description = description;
        request.payload = payload;
        request.currency = currency;
        request.prices = prices;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.provider_token = provider_token;
        request.max_tip_amount = max_tip_amount;
        request.suggested_tip_amounts = suggested_tip_amounts;
        request.start_parameter = start_parameter;
        request.provider_data = provider_data;
        request.photo_url = photo_url;
        request.photo_size = photo_size;
        request.photo_width = photo_width;
        request.photo_height = photo_height;
        request.need_name = need_name;
        request.need_phone_number = need_phone_number;
        request.need_email = need_email;
        request.need_shipping_address = need_shipping_address;
        request.send_phone_number_to_provider = send_phone_number_to_provider;
        request.send_email_to_provider = send_email_to_provider;
        request.is_flexible = is_flexible;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendInvoice";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendInvoice(const SendInvoiceRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.title = title;
        request.description = description;
        request.payload = payload;
        request.currency = currency;
        request.prices = prices;
        request.message_thread_id = message_thread_id;
        request.direct_messages_topic_id = direct_messages_topic_id;
        request.provider_token = provider_token;
        request.max_tip_amount = max_tip_amount;
        request.suggested_tip_amounts = suggested_tip_amounts;
        request.start_parameter = start_parameter;
        request.provider_data = provider_data;
        request.photo_url = photo_url;
        request.photo_size = photo_size;
        request.photo_width = photo_width;
        request.photo_height = photo_height;
        request.need_name = need_name;
        request.need_phone_number = need_phone_number;
        request.need_email = need_email;
        request.need_shipping_address = need_shipping_address;
        request.send_phone_number_to_provider = send_phone_number_to_provider;
        request.send_email_to_provider = send_email_to_provider;
        request.is_flexible = is_flexible;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.suggested_post_parameters = suggested_post_parameters;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendInvoice";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<std::string> Api::createInvoiceLink(const std::string& title, const std::string& description, const std::string& payload, const std::string& currency, const std::vector<LabeledPrice::Ptr>& prices, const std::string& business_connection_id, const std::string& provider_token, std::int64_t subscription_period, std::int64_t max_tip_amount, const std::vector<std::int64_t>& suggested_tip_amounts, const std::string& provider_data, const std::string& photo_url, std::int64_t photo_size, std::int64_t photo_width, std::int64_t photo_height, bool need_name, bool need_phone_number, bool need_email, bool need_shipping_address, bool send_phone_number_to_provider, bool send_email_to_provider, bool is_flexible) const {
        TgBot::CreateInvoiceLinkRequest request{};
        request.title = title;
        request.description = description;
        request.payload = payload;
        request.currency = currency;
        request.prices = prices;
        request.business_connection_id = business_connection_id;
        request.provider_token = provider_token;
        request.subscription_period = subscription_period;
        request.max_tip_amount = max_tip_amount;
        request.suggested_tip_amounts = suggested_tip_amounts;
        request.provider_data = provider_data;
        request.photo_url = photo_url;
        request.photo_size = photo_size;
        request.photo_width = photo_width;
        request.photo_height = photo_height;
        request.need_name = need_name;
        request.need_phone_number = need_phone_number;
        request.need_email = need_email;
        request.need_shipping_address = need_shipping_address;
        request.send_phone_number_to_provider = send_phone_number_to_provider;
        request.send_email_to_provider = send_email_to_provider;
        request.is_flexible = is_flexible;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createInvoiceLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::string>(response);
    }

    coro::task<std::string> Api::createInvoiceLink(const CreateInvoiceLinkRequest& request) const {
         request{};
        request.title = title;
        request.description = description;
        request.payload = payload;
        request.currency = currency;
        request.prices = prices;
        request.business_connection_id = business_connection_id;
        request.provider_token = provider_token;
        request.subscription_period = subscription_period;
        request.max_tip_amount = max_tip_amount;
        request.suggested_tip_amounts = suggested_tip_amounts;
        request.provider_data = provider_data;
        request.photo_url = photo_url;
        request.photo_size = photo_size;
        request.photo_width = photo_width;
        request.photo_height = photo_height;
        request.need_name = need_name;
        request.need_phone_number = need_phone_number;
        request.need_email = need_email;
        request.need_shipping_address = need_shipping_address;
        request.send_phone_number_to_provider = send_phone_number_to_provider;
        request.send_email_to_provider = send_email_to_provider;
        request.is_flexible = is_flexible;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/createInvoiceLink";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::string>(response);
    }

    coro::task<bool> Api::answerShippingQuery(const std::string& shipping_query_id, bool ok, const std::vector<ShippingOption::Ptr>& shipping_options, const std::string& error_message) const {
        TgBot::AnswerShippingQueryRequest request{};
        request.shipping_query_id = shipping_query_id;
        request.ok = ok;
        request.shipping_options = shipping_options;
        request.error_message = error_message;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerShippingQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::answerShippingQuery(const AnswerShippingQueryRequest& request) const {
         request{};
        request.shipping_query_id = shipping_query_id;
        request.ok = ok;
        request.shipping_options = shipping_options;
        request.error_message = error_message;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerShippingQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::answerPreCheckoutQuery(const std::string& pre_checkout_query_id, bool ok, const std::string& error_message) const {
        TgBot::AnswerPreCheckoutQueryRequest request{};
        request.pre_checkout_query_id = pre_checkout_query_id;
        request.ok = ok;
        request.error_message = error_message;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerPreCheckoutQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::answerPreCheckoutQuery(const AnswerPreCheckoutQueryRequest& request) const {
         request{};
        request.pre_checkout_query_id = pre_checkout_query_id;
        request.ok = ok;
        request.error_message = error_message;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/answerPreCheckoutQuery";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<StarTransactions::Ptr> Api::getStarTransactions(std::int64_t offset, std::int64_t limit) const {
        TgBot::GetStarTransactionsRequest request{};
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getStarTransactions";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<StarTransactions::Ptr>(response);
    }

    coro::task<StarTransactions::Ptr> Api::getStarTransactions(const GetStarTransactionsRequest& request) const {
         request{};
        request.offset = offset;
        request.limit = limit;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getStarTransactions";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<StarTransactions::Ptr>(response);
    }

    coro::task<bool> Api::refundStarPayment(std::int64_t user_id, const std::string& telegram_payment_charge_id) const {
        TgBot::RefundStarPaymentRequest request{};
        request.user_id = user_id;
        request.telegram_payment_charge_id = telegram_payment_charge_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/refundStarPayment";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::refundStarPayment(const RefundStarPaymentRequest& request) const {
         request{};
        request.user_id = user_id;
        request.telegram_payment_charge_id = telegram_payment_charge_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/refundStarPayment";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::editUserStarSubscription(std::int64_t user_id, const std::string& telegram_payment_charge_id, bool is_canceled) const {
        TgBot::EditUserStarSubscriptionRequest request{};
        request.user_id = user_id;
        request.telegram_payment_charge_id = telegram_payment_charge_id;
        request.is_canceled = is_canceled;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editUserStarSubscription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::editUserStarSubscription(const EditUserStarSubscriptionRequest& request) const {
         request{};
        request.user_id = user_id;
        request.telegram_payment_charge_id = telegram_payment_charge_id;
        request.is_canceled = is_canceled;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/editUserStarSubscription";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }


    // Telegram Passport

    coro::task<bool> Api::setPassportDataErrors(std::int64_t user_id, const std::vector<PassportElementError::Ptr>& errors) const {
        TgBot::SetPassportDataErrorsRequest request{};
        request.user_id = user_id;
        request.errors = errors;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setPassportDataErrors";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }

    coro::task<bool> Api::setPassportDataErrors(const SetPassportDataErrorsRequest& request) const {
         request{};
        request.user_id = user_id;
        request.errors = errors;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setPassportDataErrors";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<bool>(response);
    }


    // Games

    coro::task<Message::Ptr> Api::sendGame(std::int64_t chat_id, const std::string& game_short_name, const std::string& business_connection_id, std::int64_t message_thread_id, bool disable_notification, bool protect_content, bool allow_paid_broadcast, const std::string& message_effect_id, ReplyParameters::Ptr reply_parameters, InlineKeyboardMarkup::Ptr reply_markup) const {
        TgBot::SendGameRequest request{};
        request.chat_id = chat_id;
        request.game_short_name = game_short_name;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendGame";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::sendGame(const SendGameRequest& request) const {
         request{};
        request.chat_id = chat_id;
        request.game_short_name = game_short_name;
        request.business_connection_id = business_connection_id;
        request.message_thread_id = message_thread_id;
        request.disable_notification = disable_notification;
        request.protect_content = protect_content;
        request.allow_paid_broadcast = allow_paid_broadcast;
        request.message_effect_id = message_effect_id;
        request.reply_parameters = reply_parameters;
        request.reply_markup = reply_markup;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/sendGame";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::setGameScore(std::int64_t user_id, std::int64_t score, bool force, bool disable_edit_message, std::int64_t chat_id, std::int64_t message_id, const std::string& inline_message_id) const {
        TgBot::SetGameScoreRequest request{};
        request.user_id = user_id;
        request.score = score;
        request.force = force;
        request.disable_edit_message = disable_edit_message;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setGameScore";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<Message::Ptr> Api::setGameScore(const SetGameScoreRequest& request) const {
         request{};
        request.user_id = user_id;
        request.score = score;
        request.force = force;
        request.disable_edit_message = disable_edit_message;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/setGameScore";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<Message::Ptr>(response);
    }

    coro::task<std::vector<GameHighScore::Ptr>> Api::getGameHighScores(std::int64_t user_id, std::int64_t chat_id, std::int64_t message_id, const std::string& inline_message_id) const {
        TgBot::GetGameHighScoresRequest request{};
        request.user_id = user_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getGameHighScores";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<GameHighScore::Ptr>>(response);
    }

    coro::task<std::vector<GameHighScore::Ptr>> Api::getGameHighScores(const GetGameHighScoresRequest& request) const {
         request{};
        request.user_id = user_id;
        request.chat_id = chat_id;
        request.message_id = message_id;
        request.inline_message_id = inline_message_id;
        json j = request;
        std::string body = j.dump();
        std::string contentType = "application/json";
        const std::string target = "/bot" + api_key_ + "/getGameHighScores";
        std::string response = co_await http_client_.makeRequest(HttpVerb::POST, target, body, contentType);
        co_return parseResponse<std::vector<GameHighScore::Ptr>>(response);
    }
}
